/**
 * A single restrained line introducing the destination.
 * Intentionally not promotional. Edit copy in content if desired.
 */
export function AnnouncementBar() {
  return (
    <div className="relative z-[60] bg-ink text-ivory">
      <p className="mx-auto max-w-shell px-6 py-2.5 text-center font-sans text-[0.68rem] uppercase tracking-eyebrow text-ivory">
        A new destination for beauty, wellness &amp; longevity — Tribeca, New York
      </p>
    </div>
  );
}
